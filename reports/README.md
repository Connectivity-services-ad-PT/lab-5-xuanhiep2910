# Reports

Thư mục chứa báo cáo Newman và bằng chứng chạy stack Compose.

Sau khi chạy `npm run test:compose` hoặc `make test-compose`, các file sau sẽ được tạo:

- `newman-lab05-compose.xml`
- `newman-lab05-compose.html`

Thêm screenshot `/health` hoặc log container vào đây trước khi nộp bài.
